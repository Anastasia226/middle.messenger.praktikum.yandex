<div class="form-item {{className}}">
    <label>{{label}}</label>
    <input
            type="{{type}}"
            placeholder="{{placeholder}}"
            name="{{name}}"
            value="{{value}}"
    />
    {{{error}}}
</div>