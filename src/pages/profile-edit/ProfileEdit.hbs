<div class="page-profile-edit">
    <div class="logout"> {{{logOut}}}</div>
    <div class="photo"><img src="{{avatarProfile}}">
        <div class="change-avatar">{{{inputFile}}}</div>
    </div>

    <div class="data-profile">
        <div class="items">
            <form id="form-edit-profile">
                {{{email}}}
                {{{login}}}
                {{{firstName}}}
                {{{lastName}}}
                {{{telephone}}}
            </form>
        </div>
        <div class="group-btn">
            <div>{{{buttonSave}}}</div>
            <div>{{{linkCancel}}}</div>
        </div>

    </div>
</div>